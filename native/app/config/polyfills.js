//let polyfills = require('react-native-browser-builtins');

//global.Buffer = require(polyfills.buffer).Buffer;
//global.assert = require(polyfills.assert);
//global.crypto = require(polyfills.crypto);
//global.process = require(polyfills.process);
